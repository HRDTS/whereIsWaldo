// This file contains the code blocks to write data to the Firebase database and Cloudstore.
// Keep this in case you want to see examples on writing firebase code.


//this code adds pokemons to the cloudstore with the four coordinates.
/*
        function writeUserData(userId, y1, y2, x1, x2) {
        const db = getDatabase();
        set(ref(db, 'usersv2Easy/' + userId), {
            y1: y1,
            y2: y2,
            x1 : x1,
            x2: x2
        });
    }

      writeUserData('location2', 65, 78, 0, 25 )
      writeUserData('location3', 24, 27, 82, 86 )
      writeUserData('location1', 72, 80, 85, 97 ) 
*/

